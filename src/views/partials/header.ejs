<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' | ' + appName : appName %></title>
    <meta name="description" content="FastCenter - High-Performance Cybersecurity Consultancy. Audits, Pentesting, and Compliance.">
    <!-- Tailwind CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="flex flex-col min-h-screen">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex">
                    <div class="flex-shrink-0 flex items-center">
                        <!-- Logo -->
                        <a href="/" class="flex items-center gap-2">
                            <!-- Placeholder Logo if image missing, or actual image -->
                            <img class="h-8 w-auto" src="/images/logo.png" alt="<%= appName %> Logo" onerror="this.style.display='none'">
                            <span class="font-bold text-xl tracking-tight text-brand-dark">
                                <%= appName %>
                            </span>
                        </a>
                    </div>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:space-x-8 items-center">
                    <a href="#services" class="border-transparent text-gray-500 hover:border-brand-light hover:text-brand-dark inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">Services</a>
                    <a href="#certifications" class="border-transparent text-gray-500 hover:border-brand-light hover:text-brand-dark inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">Certifications</a>
                    <a href="#contact" class="btn-primary text-sm px-4 py-2 ml-4">Contact Us</a>
                </div>
            </div>
        </div>
    </nav>
